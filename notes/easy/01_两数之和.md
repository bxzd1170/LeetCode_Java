# 001.两数之和
## 解题思路
### 暴力解法
两层循环依次遍历数组，判断两数之和是否等于目标值，匹配成功直接返回下标。
时间复杂度：O(n2) ,空间复杂度：O（1）

### 哈希表优化解法
创建哈希表存储数值与对应下标；遍历数组时先计算所需补数，若哈希表存在补数则直接返回下标，不存在则将当前数存入哈希表。
时间复杂度： O(n)，数组只遍历 1 次，哈希查找、存入都是 O (1);
空间复杂度： O(n)，最坏情况哈希表存储全部数组元素（空间换时间）；